# CLAUDE.md

> Claude Code が毎セッション自動参照するプロジェクトコンテキスト。
> 詳細は `DESIGN.md` を参照。

---

## プロジェクト概要

**まちのおすそわけ** — 都城・三股・曽於の応援グルメサイト

飲食店の利用が、自動的に地元の子ども食堂や少年団への支援に変わる仕組み。
ホットペッパー/食べログに代わる地域密着型プラットフォーム。

- ドメイン: `osusowake.city`
- サブタイトル: 都城・三股・曽於の応援グルメ
- タグライン: 食べて、まちをおすそわけ。

---

## 技術スタック (固定)

| レイヤー | 技術 |
|---|---|
| フロントエンド | Next.js 14+ (App Router) + TypeScript |
| スタイリング | Tailwind CSS + shadcn/ui |
| データベース | PostgreSQL 16 (ConoHa VPS 上) |
| ORM | Drizzle ORM |
| 認証 | NextAuth.js v5 (Auth.js) |
| 地図 | Leaflet + OpenStreetMap |
| 画像保存 | VPSローカル + Nginx静的配信 |
| デプロイ | Docker + docker-compose |
| リバプロ・SSL | Nginx + Let's Encrypt |
| ホスティング | ConoHa VPS |

**勝手にスタックを変更しないこと**。代替案を提案する時は必ず確認を取る。

---

## 階層・料金 (固定値、変更禁止)

| Tier | 業態 | 店負担/枚 | 客pt | 寄付 | 運営 |
|---|---|---|---|---|---|
| `light` | 喫茶・弁当・FF | ¥20 | ¥8 | ¥8 | ¥4 |
| `standard` | ラーメン・定食・カフェ | ¥40 | ¥15 | ¥17 | ¥8 |
| `group` | 居酒屋・焼肉・寿司 | ¥80 | ¥30 | ¥35 | ¥15 |
| `premium` | 割烹・コース | ¥150 | ¥55 | ¥65 | ¥30 |

「全部寄付」選択時は、客pt分だけ店負担が減る (案A採用)。

---

## ブランド方針

- **トーン**: 和モダン、温かみ、控えめな品格
- **フォント**: ロゴはセリフ体 (明朝系)、本文はsans-serif
- **カラー**: 温白色をベースに、朱色 or 藍色をアクセント
- **避ける**: 派手なグラデーション、ポップすぎる装飾、Hot Pepper風の濃色背景

---

## ディレクトリ慣習

```
osusowake_city/
├── DESIGN.md              # 詳細設計書 (随時更新)
├── CLAUDE.md              # このファイル (固定方針)
├── README.md              # セットアップ手順
├── docker-compose.yml
├── Dockerfile
├── nginx/
├── app/                   # Next.js (App Router)
│   ├── (public)/          # 客向けページ
│   ├── store/             # 店主向け
│   ├── org/               # 団体向け
│   ├── admin/             # 運営向け
│   └── api/
├── db/
│   ├── schema.ts          # Drizzle schema
│   └── migrations/
├── components/
├── lib/
└── data/                  # アップロード画像等 (gitignore)
```

---

## ペルソナ (常に意識)

- **客**: スマホ中心、20〜60代、地元利用者
- **店主**: PC/タブレット、40〜70代、ITに強くない
- **団体**: 月数回ログイン、シンプルさ最重要
- **運営**: あなた本人

特に**店主は IT が苦手**な前提で UI を作る。クリック数を減らす、専門用語を避ける。

---

## ワーキングルール

### 必ず守る
- 既存ファイルを編集する前に、必ず内容を確認する
- 階層の数値や料金ロジックを勝手に変更しない
- 個人情報・銀行口座情報は暗号化、ログに出さない
- 新しいパッケージを入れる前に必要性を提案する

### 推奨フロー
1. 新機能 → `DESIGN.md` の該当セクションを更新
2. 実装 → コミット (Conventional Commits 形式)
3. 設計判断で迷ったら、ユーザーに確認

### コミットメッセージ
- `feat:` 新機能
- `fix:` バグ修正
- `docs:` ドキュメント更新
- `refactor:` 動作を変えないリファクタ
- `chore:` 設定・依存関係更新

---

## 進捗トラッキング

実装が完了したスプリントは `DESIGN.md` セクション8に `[完了 YYYY-MM-DD]` を追記する。
未着手のオープン論点は `DESIGN.md` セクション12を更新。

---

## 開発環境

- OS: macOS (開発), Linux (本番 ConoHa VPS)
- Node.js: 20 LTS 以上
- pnpm 推奨 (npm/yarn でも可)

ローカル起動:
```bash
docker compose up -d
pnpm dev
```

本番デプロイは `docs/deploy.md` 参照 (Sprint 1 で作成)。

---

## 注意事項

- ホットペッパーや食べログの UI を真似ない。設計書のオリジナル方針で。
- 「寄付」という用語は法務上慎重に扱う。「支援金」「協賛金」を基本とする。
- 子ども食堂・少年団の名前を扱う時は、事前同意が前提。テストデータでもダミー名を使う。
